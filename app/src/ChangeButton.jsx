import React from 'react'

const ChangeButton = ({changeColorRandomly}) => {
  return (
    <><button onClick={changeColorRandomly}>Change Color Randomly</button></>
  )
}

export default ChangeButton;
